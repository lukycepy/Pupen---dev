# Checklist
- [x] Public web má sjednocený vizuální jazyk (UI kit) na všech stránkách a podstránkách
- [x] Admin dashboard má sjednocené tabulky/search/CRUD modaly a konzistentní feedback
- [x] Členská sekce má sjednocený layout/UX (sidebar, taby, dokumenty, directory + mapa)
- [x] Nové stránky mají konzistentní UI stavy (loading/empty/error) a mobilní layout
- [x] SOS export vrací správné Content-Type a soubory jsou použitelné (CSV/VCF)
- [x] SOS offline fallback funguje (bez sítě se zobrazí cache + informace o stáří)
- [x] Lost&Found detail funguje end-to-end (list → detail → claim → kontakt)
- [x] Member directory mapa neshazuje render (chybějící lat/lon/city) a je čitelná na mobile
- [x] Výroční zprávy filtruje správné dokumenty a zobrazuje funkční download odkazy
- [x] Výbor stránka zobrazuje smysluplnou sadu členů (role filtr + fallback)
- [x] Všechny nové UI texty jsou v dictionaries (cs/en) bez hardcoded řetězců
- [x] Všechny DB změny jsou pokryté migracemi v `/migrace` a jsou idempotentní
- [x] `npm run db:migrate` + `npm run db:drift-check` projdou na cílové DB
- [x] `npx tsc --noEmit` a `npm run build` projdou
